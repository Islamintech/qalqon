# Optional: the read-only dashboard (web/). The bot itself does not need these,
# so it stays lean if you never run the panel.
-r requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Form parsing, for the token sign-in used when no domain is available.
python-multipart==0.0.20
